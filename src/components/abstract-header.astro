---
import { parseDataAttrs } from "@components/utils/property-utils"

interface Props {
    className?: string
    dataset?: DOMStringMap
    id?: string
}

const { className, dataset, id } = Astro.props
---

<header class={className} id={id} {...parseDataAttrs(dataset)} role="banner">
    <slot />
</header>
