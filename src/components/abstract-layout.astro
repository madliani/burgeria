---
import { parseDataAttrs } from "@components/utilities/property-utilities"

interface Props {
    className?: string
    dataset?: DOMStringMap
    id?: string
    language?: string
}

const { className, dataset, id, language } = Astro.props
---

<!doctype html>
<html lang={language}>
    <head>
        <slot name="head" />
    </head>

    <body class={className} id={id} {...parseDataAttrs(dataset)}>
        <slot />
    </body></html
>
