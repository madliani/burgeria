---
interface Props {
    className?: string
    lang?: string
}

const { className, lang } = Astro.props
---

<!doctype html>
<html lang={lang}>
    <head>
        <slot name="head" />
    </head>

    <body class={className}>
        <slot name="noscript" />

        <slot name="header" />

        <slot />

        <slot name="footer" />
    </body></html
>
