---
interface Props {
    className?: string
    href?: string
    id?: string
    title: string
}

const { className, href, id, title } = Astro.props
---

{
    href !== undefined ? (
        <a href={href}>
            <button class={className}>
                <span>{title}</span>
            </button>
        </a>
    ) : (
        <button class={className} id={id}>
            <span>{title}</span>
        </button>
    )
}
