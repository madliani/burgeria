---
interface Props {
    className?: string
    id?: string
    title?: string
}

const { className, id, title } = Astro.props
---

<ul aria-label={title} class={className} id={id} title={title}>
    <slot />
</ul>
