---
import { parseDataAttrs } from "@components/utilities/property-utilities"

interface Props {
    className?: string
    dataset?: DOMStringMap
    id?: string
}

const { className, dataset, id } = Astro.props
---

<noscript class={className} id={id} {...parseDataAttrs(dataset)}>
    <slot />
</noscript>
