---
interface Props {
    lang?: string
}

const { lang } = Astro.props
---

<!doctype html>
<html lang={lang}>
    <head>
        <slot name="head" />
    </head>

    <body class="body">
        <slot name="header" />

        <slot name="left-sidebar" />

        <slot />

        <slot name="footer" />
    </body></html
>
