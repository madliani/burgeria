---
import { parseDataAttrs } from "@pages/components/utilities/property-utilities"

interface Props {
    className?: string
    dataset?: DOMStringMap
}

const { className, dataset } = Astro.props
---

<footer class={className} {...parseDataAttrs(dataset)}>
    <slot />
</footer>
