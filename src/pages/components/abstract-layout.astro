---
import { parseDataAttrs } from "@pages/components/utilities/property-utilities"

interface Props {
    className?: string
    dataset?: DOMStringMap
    lang?: string
}

const { className, dataset, lang } = Astro.props
---

<!doctype html>
<html lang={lang}>
    <head>
        <slot name="head" />
    </head>

    <body class={className} {...parseDataAttrs(dataset)}>
        <slot />
    </body></html
>
