---
interface Props {
    alt: string
    className?: string
    src: string
}

const { alt, className, src } = Astro.props
---

<img alt={alt} aria-label={alt} class={className} src={src} title={alt} />
