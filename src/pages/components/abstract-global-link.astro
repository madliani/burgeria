---
import { parseDataAttrs } from "@pages/components/utilities/property-utilities"

interface Props {
    className?: string
    dataset?: DOMStringMap
    href: string
    newTab?: boolean
    title?: string
}

const { className, dataset, href, newTab = true, title } = Astro.props
---

{
    newTab ? (
        <a
            aria-label={title}
            class={className}
            href={href}
            referrerpolicy="no-referrer"
            rel="noopener noreferrer"
            target="_blank"
            title={title}
            {...parseDataAttrs(dataset)}
        >
            <slot />
        </a>
    ) : (
        <a
            aria-label={title}
            class={className}
            href={href}
            referrerpolicy="no-referrer"
            rel="noopener noreferrer"
            title={title}
            {...parseDataAttrs(dataset)}
        >
            <slot />
        </a>
    )
}
