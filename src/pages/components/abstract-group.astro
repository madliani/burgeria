---
interface Props {
    className?: string
    id?: string
    title?: string
}

const { className, title } = Astro.props
---

<div aria-label={title} class={className} title={title}>
    <slot />
</div>
